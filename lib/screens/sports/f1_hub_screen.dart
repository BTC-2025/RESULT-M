import 'package:flutter/material.dart';

/// F1 has been removed. This is a placeholder stub.
class F1HubScreen extends StatelessWidget {
  const F1HubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Formula 1')),
      body: const Center(child: Text('F1 is not available in this version.')),
    );
  }
}
